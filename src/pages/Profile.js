import React from 'react';
import { withRoot } from '../contexts/RootContext';

const Profile = () =>{
    return(
        <div>
            프로필
        </div>
    )
}

export default withRoot(Profile);